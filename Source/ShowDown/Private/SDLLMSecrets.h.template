// SDLLMSecrets.h.template
//
// 사용법:
//   1) 이 파일을 같은 폴더에 SDLLMSecrets.h 로 복사한다.
//   2) 아래 SHOWDOWN_LLM_API_KEY 값을 본인 OpenAI 키로 바꾼다.
//   3) SDLLMSecrets.h 는 .gitignore 에 등록되어 있어 커밋되지 않는다.
//
// SDLLMSecrets.h 가 없으면 코드는 자동으로 OPENAI_API_KEY 환경변수를 사용한다.
#pragma once

#define SHOWDOWN_LLM_API_KEY TEXT("sk-your-key-here")
